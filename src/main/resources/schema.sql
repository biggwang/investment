/*-- used in tests that use HSQL
create table IF NOT EXISTS product (
    product_id INTEGER PRIMARY KEY,
    title VARCHAR(200),
    total_investing_amount INTEGER,
    status VARCHAR(20),
    started_at TIMESTAMP,
    finished_at TIMESTAMP
);*/